DATABASE_URL="postgres://signage:signage@localhost:5433/signage"
WORDPRESS_BASE_URL="https://www.wordonthestreetbooks.com"
SIGNAGE_WEBHOOK_SECRET="change-me"

# Only used by docker-compose.yml (the full web+worker+db stack) — not
# needed for local dev against docker-compose.dev.yml, which hardcodes a
# throwaway local password.
# POSTGRES_PASSWORD="set-a-real-password"
